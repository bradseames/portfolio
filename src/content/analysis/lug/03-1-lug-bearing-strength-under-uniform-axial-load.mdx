import { Divider, Flex, Group, Card } from '@mantine/core'
import { LineChart } from '@mantine/charts'
import { KChart, KbChart } from '../../../components/LugCalculator/coeff_data'
export { KChart }
export { KbChart }

### 9.3.1 Lug Bearing Strength Under Uniform Axial Load

<Divider />

The bearing stresses and loads for lug failure involving bearing, shear-tearout, or hoop tension
in the region forward of the net-section in Figure 9-1 are determined from the equations below,
with an allowable load coefficient $ (\text{K}) $ determined from Figures 9-2 and 9-3. For values
of e/D less than 1.5, lug failures are likely to involve shear-out or hoop tension; and for
values of e/D greater than 1.5, the bearing is likely to be critical. Actual lug failures may
involve more than one failure mode, but such interaction effects are accounted for in the
values of $ \text{K} $. The lug ultimate bearing stress
$ (\text{F}_{\text{bru}_{\text{L}}}) $ is

$$
\quad \text{F}_{\text{bru}_{\text{L}}} = \text{K} \ \frac{a}{D} \ \text{F}_\text{tux}
\quad \text{(9-1a)}
$$

$ e/D \geq 1.5: $

$$
\quad \text{F}_{\text{bru}_{\text{L}}} = \text{K} \ \text{F}_\text{tux}
\quad \text{(9-1b)}
$$

The graph in Figure 9-2 applies only to cases where D/t is 5 or less, which covers most of the
cases. If D/t is greater than 5, there is a reduction in strength which can be approximated by
the curves in Figure 9-3. The lug yield bearing stress $ (\text{F}_{bry_L}) $ is

$ e/D \lt 1.5: $

$$
\quad \text{F}_{\text{bry}_{\text{L}}} = \text{K} \ \frac{a}{D} \ \text{F}_\text{tyx}
\quad \text {(9-2a)}
$$

$ e/D \geq 1.5: $

$$
\quad \text{F}_{\text{bry}_{\text{L}}}  = \text{K} \ \text{F}_\text{tyx}
\quad \text{(9-2b)}
$$

<Card>
  <Group bg="white" c="black" mx="auto" w='100%'>
    <KChart h={400}
            w={'100%'}
            style={{
              padding: 20,
              paddingBottom: 0
            }} />

    <p style={{ margin: 'auto', maxWidth: 400, textAlign: 'center' }}>
      Figure 9-2. Allowable Uniform Axial Load Coefficient
    </p>
  </Group>
</Card>


<Card>
  <Group bg="white" c="black" mx="auto" w='100%'>
    <KbChart h={400}
             w={'100%'}
             style={{
               padding: 20,
               paddingBottom: 0
             }} />

    <p style={{ margin: 'auto', maxWidth: 400, textAlign: 'center' }}>
      Figure 9-3. Bearing Efficiency Factors for Lugs Aluminum Alloy,
      and Alloy Steel with $\text{F}_{tu} \lt$ 160 KSI
    </p>
  </Group>
</Card>



The allowable lug ultimate bearing load
$ (\text{P}_{\text{bru}_{\text{L}}}) $ for lug failure in bearing, shear-out, or hoop tension is

$ \text{F}_\text{tux} \leq \ 1.304 \ \text{F}_\text{tyx}: $
$$
\quad \text{P}_{\text{bru}_{\text{L}}} = \text{F}_{\text{bru}_{\text{L}}} \ \text{D} \ \text{t}
\quad \text{(9-3a)}
$$

$ \text{F}_\text{tux} \gt \ 1.304 \ \text{F}_\text{tyx}: $

$$
\quad \text{P}_{\text{bru}_{\text{L}}} = 1.304 \text{F}_{\text{bry}_{\text{L}}} \ \text{D} \
\text{t}
\quad \text{(9-3b)}
$$

$ \text{P}_{\text{bru}_{\text{L}}}/\text{Dt} $ should not exceed either
$ \text{F}_{\text{bru}_{\text{L}}} \text{ or } 1.304 \text{F}_{\text{bry}_{\text{L}}} $, where
$ \text{F}_{\text{bru}_{\text{L}}} \text{ and } \text{F}_{\text{bry}_{\text{L}}} $ are the
allowable ultimate and yield bearing stresses for the lug material for e/D = 2.0, as given in
MIL-HDBK-5 or other applicable specification.

Equations (9-3a) and (9-3b) apply only if the load is uniformly distributed across the lug
thickness. If the pin is too flexible and bends excessively, the load on the lug will tend to
peak up near the shear faces and possibly cause premature failure of the lug.

A procedure to check the pin bending strength in order to prevent premature lug failure is given
in Section 9.4 entitled "Double Shear Joint Strength Under Uniform Axial Load.. "
