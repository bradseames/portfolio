import {useParams} from 'react-router-dom';
// import {getMDXBySlug} from './subjectAPI.ts'; // Your logic to import/require MDX by slug
// import {IntegrationByPartsVisualization} from '../../components/IntegrationByPartsVisualization';

// const components = {IntegrationByPartsVisualization};

export default function SubjectPage() {
  // const {subject, concept} = useParams();
  // const MDXContent = getMDXBySlug(subject, concept); // Returns the right MDX component
  
  return (
    <div>
      {/*{MDXContent ? (*/}
      {/*  <MDXContent components={components}/>*/}
      {/*) : (*/}
      {/*  <div>Concept not found.</div>*/}
      {/*)}*/}
    </div>
  );
}
